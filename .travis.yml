language: python
python: 2.7

sudo: required

services:
  - docker

env:
  - TOX_ENV=py27-ansible19
  - TOX_ENV=py27-ansible20

install:
  - pip install tox

script:
  - docker run busybox ls -rtl
  - tox -e $TOX_ENV

after_success:
  - coveralls
